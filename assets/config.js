/* Deployment configuration.
 *
 * Set this to the deployed Cloudflare Worker origin once it exists, e.g.
 * "https://portfolio-api.dotbion.workers.dev". While it is empty the add-ons
 * stay dormant, so the site is safe to deploy before the backend is up.
 */
window.PORTFOLIO_API = 'https://portfolio-api.nc3610.workers.dev';